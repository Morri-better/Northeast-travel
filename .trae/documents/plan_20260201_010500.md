# 去除验证码属性但不影响功能

## 1. 修改RegisterRequest请求类
在 [RegisterRequest.java](file:///d:\东北旅游\Northeast-travel\src\main\java\com\example\travelservice\dto\request\RegisterRequest.java) 中：
- 去除captcha字段
- 去除captcha的@NotBlank验证注解

## 2. 修改UserController控制器
在 [UserController.java](file:///d:\东北旅游\Northeast-travel\src\main\java\com\example\travelservice\controller\UserController.java) 中：
- 去除register方法的HttpSession参数
- 调用userServer.register(request)时不再传入session

## 3. 修改UserServer服务类
在 [UserServer.java](file:///d:\东北旅游\Northeast-travel\src\main\java\com\example\travelservice\service\UserServer.java) 中修改register方法：
- 去除captcha字段获取
- 去除验证码校验逻辑
- 去除session.getAttribute和session.removeAttribute调用
- 保留手机号和用户名唯一性检查
- 保留密码加密和用户插入逻辑

## 文件清单
- 修改：`dto/request/RegisterRequest.java` - 去除captcha字段
- 修改：`controller/UserController.java` - 去除HttpSession参数
- 修改：`service/UserServer.java` - 去除验证码校验逻辑